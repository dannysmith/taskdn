---
title: 'Getting Started'
description: 'Learn the fundamentals of Taskdn'
---

import { Badge, LinkCard, CardGrid } from '@astrojs/starlight/components'
import { Image } from 'astro:assets'
import desktopScreenshot from '@assets/desktop-1.png'
import obsidianScreenshot from '@assets/obsidian-screenshot-1.png'

Most task management apps force a choice: use something like Notion and get powerful features but lose ownership of your data, or use plain files and miss out on good UIs. Taskdn is a different approach—your tasks live as markdown files on disk, but you get proper tools to work with them.

The system stores tasks, projects, and areas as markdown files with YAML frontmatter. Three directories, one file per entity, that's it. Keep them in an [Obsidian](https://obsidian.md) vault, a git repo, or just a folder on your desktop. Since everything is just files, you can edit them in any text editor, version them, back them up however you like, or build your own tools on top.

But raw files aren't great for actually *doing* task management. That's where the Taskdn products come in—interfaces designed for specific contexts, all reading and writing the same files.

## The Suite

|     | Product                                     | Status                                       | Description                                        |
| --- | ------------------------------------------- | -------------------------------------------- | -------------------------------------------------- |
| 🖥️  | [Desktop App](/desktop/overview)            | <Badge text="Coming Soon" variant="note" />  | Day-to-day task management with a beautiful UI     |
| ⌨️  | [CLI](/cli/overview)                        | <Badge text="Available" variant="success" /> | Terminal interface for humans and AI agents        |
| 💎  | [Obsidian Plugin](/obsidian/plugin)         | <Badge text="Available" variant="success" /> | View and manage tasks within Obsidian              |
| 🤖  | [Claude Code Plugin](/claude-code/overview) | <Badge text="Available" variant="success" /> | Skill and commands for AI-assisted task management |

## Core Concepts

Your data lives in three directories: `tasks/`, `projects/`, and `areas/`. Each entity gets its own markdown file with YAML frontmatter at the top and freeform notes in the body. That's the whole system.

Many people keep these directories inside an Obsidian vault, which works well—you can link between tasks and notes, use Obsidian's search, and the Taskdn plugin adds task-specific features. But Obsidian isn't required. The files work anywhere.

This file-based approach also means AI tools can work with your tasks directly. [Claude Code](https://docs.anthropic.com/en/docs/claude-code) (or any agent that can read files) can query your tasks, create new ones, or help with planning—no API keys or integrations required. The CLI has an `--ai` mode that outputs structured data designed for LLMs.

### The Hierarchy

Taskdn uses three entity types, loosely based on [GTD](https://gettingthingsdone.com/) and [PARA](https://fortelabs.com/blog/para/):

- **Areas** are ongoing areas of responsibility—things like "Work", "Health", or "Client: Acme Corp". They don't end.
- **Projects** have a defined goal and finish when you're done. They can belong to an area.
- **Tasks** are the actual things you do. A task can belong to a project, to an area directly, or stand alone.

So "Work" (area) might contain "Q1 Planning" (project), which contains "Review budget spreadsheet" (task). Or "Call the dentist" might just sit under "Health" without needing a project.

### Tasks

A task represents a single actionable item. Tasks live in your configured `tasks/` directory.

```yaml title="tasks/review-quarterly-report.md"
---
title: Review quarterly report
status: in-progress
scheduled: 2025-01-14
projects:
  - '[[Q1 Planning]]'
---
## Notes

Key points to review:
  - Revenue projections
  - Budget allocations
```

#### Task Statuses

| Status                                         | Meaning                                   |
| ---------------------------------------------- | ----------------------------------------- |
| <Badge text="inbox" variant="note" />          | Newly captured, not yet processed         |
| <Badge text="icebox" variant="note" />         | Intentionally deferred indefinitely       |
| <Badge text="ready" variant="default" />       | Processed and ready to be worked on       |
| <Badge text="in-progress" variant="caution" /> | Currently being worked on                 |
| <Badge text="blocked" variant="danger" />      | Cannot proceed due to external dependency |
| <Badge text="dropped" variant="danger" />      | Abandoned, will not be completed          |
| <Badge text="done" variant="success" />        | Completed successfully                    |

### Projects

A project represents a collection of related tasks with a defined end goal. Projects live in your configured `projects/` directory.

```yaml title="projects/q1-planning.md"
---
title: Q1 Planning
status: in-progress
area: '[[Work]]'
description: Quarterly planning and budget review for Q1 2025.
---
## Overview

This project covers all Q1 planning activities...
```

#### Project Statuses

| Status                                         | Meaning                          |
| ---------------------------------------------- | -------------------------------- |
| <Badge text="planning" variant="note" />       | Still being scoped or planned    |
| <Badge text="ready" variant="default" />       | Planned and ready to begin       |
| <Badge text="in-progress" variant="caution" /> | Active work is happening         |
| <Badge text="paused" variant="caution" />      | Temporarily on hold              |
| <Badge text="blocked" variant="danger" />      | Cannot proceed due to dependency |
| <Badge text="done" variant="success" />        | Completed                        |

### Areas

An area represents an ongoing area of responsibility. Areas live in your configured `areas/` directory.

```yaml title="areas/work.md"
---
title: Work
description: Professional responsibilities and career development.
---
## Context

Current role, key responsibilities, and long-term goals...
```

Areas don't have workflow statuses—they're just `active` or `archived`. Most of the time you won't think about area status at all.

## The Products

Different contexts need different tools. You might use the desktop app when you're focused on getting things done, switch to the CLI when you want Claude to help with a weekly review, and use the Obsidian plugin when you're working on project notes and want to check off a task without leaving your document.

### Desktop App

Think Things or Todoist, but reading your markdown files. The desktop app is where you do most of your day-to-day task management—capture quick thoughts, plan your day, work through your lists. It's designed to feel fast and get out of your way.

<Image src={desktopScreenshot} alt="Taskdn desktop app showing the Today view" />

<LinkCard
  title="Desktop App Documentation"
  href="/desktop/overview"
  description="Learn about the desktop app's features and interface"
/>

### CLI

The CLI has two faces. For humans, it's a fast way to query and update tasks from the terminal—nice formatting, fuzzy search, natural language dates (`--due tomorrow`). For AI agents, add `--ai` and it outputs structured markdown that LLMs can parse easily.

{/* TODO: Add screenshot showing CLI commands and output */}

<div style="background: var(--sl-color-gray-6); padding: 3rem; text-align: center; border-radius: 0.5rem; margin: 1rem 0;">
  <em>Screenshot coming soon</em>
</div>

The `context` command is particularly useful for AI workflows—it returns an entity plus all its relationships, so an agent can understand a project's full picture in one call.

<LinkCard
  title="CLI Documentation"
  href="/cli/overview"
  description="Learn how to use the CLI for task management"
/>

### Obsidian Plugin

If you use Obsidian, the plugin makes task links show up as widgets with status, due date, and project info. Click to change status without opening the file. You can also turn any checklist item into a proper task file.

<Image src={obsidianScreenshot} alt="Taskdn tasks rendered as interactive widgets in Obsidian" />

<LinkCard
  title="Obsidian Plugin Documentation"
  href="/obsidian/plugin"
  description="Set up the Obsidian integration"
/>

### Claude Code Plugin

Load the skill and Claude knows how to work with your tasks. Ask it what's overdue, have it create tasks as you work, or get help planning your week. The `/tdn:today` command shows what needs attention today.

{/* TODO: Add GIF/video showing Claude Code interaction */}

<div style="background: var(--sl-color-gray-6); padding: 3rem; text-align: center; border-radius: 0.5rem; margin: 1rem 0;">
  <em>Demo video coming soon</em>
</div>

<LinkCard
  title="Claude Code Plugin Documentation"
  href="/claude-code/overview"
  description="Set up AI-assisted task management"
/>
