# Task: State Management & Refactor

Now we have pulled in the React UI, we need to refactor it to fit as much as possible with the current project setup. We haven't yet decided on the Rust backend, and so we may not be able to do loads of work on state management conversion here. That might need to wait for the next task.

This is an opportunity to review the code base as is and get ready for what's coming next as far as possible. It's also an opportunity to review the conformance of the current React front end with the various guidance that we have in `docs/developer` which was pulled in from tauri-template.
