# Task: Rust Backend

Read `/docs/product-overviews/desktop/desktop-data-architecture-research.md` We now need to work iteratively to design the best possible system for the Rust backend, the best way to build and maintain the index, the TypeScript interface that we want to use with that from React And crucially how we're going to manage state and the requisite UI updates using tanstackQuery.
