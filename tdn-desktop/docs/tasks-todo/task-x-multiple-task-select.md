# Task: Multiple Task Select

It should be possible to select multiple tasks by either command clicking on many of them or by Shift clicking to select a range. When multiple tasks are selected, keyboard shortcuts, context menus, and command palette commands should operate on all selected tasks. Dragging should move all selected tasks together. That's basically it. 
